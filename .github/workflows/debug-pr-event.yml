name: Debug pr event
on:
  pull_request: 
    branches:
      - main
    types:
      - closed

  
jobs:
  metadata:
    runs-on: ubuntu-latest
    steps:
      - name: metadata
        run: |
          echo "merged: ${{ github.event.pull_request.merged }}"
    
    